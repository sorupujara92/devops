
spring.datasource.driver-class-name=org.postgresql.Driver

spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST:https://10.1.185.160/}:${POSTGRES_PORT:5432}/${POSTGRES_DB:tasks}

spring.datasource.username=${POSTGRES_USER:tasks_usr}

spring.datasource.password=${POSTGRES_PASSWORD:test}

spring.jpa.show-sql=true

spring.jpa.generate-ddl=true

spring.jpa.hibernate.ddl-auto=update

spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
#
#auth0.audience=${API_IDENTIFIER}
#
    #spring.security.oauth2.resourceserver.jwt.issuer-uri=${ISSUER_URI}








server.port=8081
